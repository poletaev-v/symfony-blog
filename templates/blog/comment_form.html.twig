{{ form_start(form, {method: 'POST', action: path('comment_new', {'postSlug': post.slug})}) }}

<div class="mb-3">
    <label for="formGroupExampleInput" class="form-label">Add comment</label>
    {{ form_widget(form.content, {attr: {rows: 10, 'class': 'form-control'}}) }}
</div>
<div class="col-12">
    <button type="submit" class="btn btn-primary">Publish comment</button>
</div>

{{ form_end(form) }}